!!!
%html
  %head
    %title Bloid
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tags
  %body
    - if flash.present?
      - flash.each_pair do |level, content|
        %div{:class => "flash #{level}"}= content
    = yield
